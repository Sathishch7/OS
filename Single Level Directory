#include <stdio.h>
#include <string.h>

int main() {
char directory[10][20];
int count = 0;

strcpy(directory[count++], "file1.txt");
strcpy(directory[count++], "file2.doc");

prinƞ("Single Level Directory:\n");
for (int i = 0; i < count; i++)
prinƞ("%s\n", directory[i]);

return 0;
}
Output:
Single Level Directory:
file1.txt
file2.doc
